# 🚀 Quick Start Guide - EnergyBrain

## Запуск за 30 секунд

```bash
# 1. Установить зависимости
npm install

# 2. Запустить в ЛУЧШЕМ режиме (Hybrid - 14.5% savings!)
npm run dev -- --mode=hybrid

# Готово! Система работает с mock LLM провайдером (без API ключей)
```

---

## 🎮 Режимы работы

### 1. 🏆 Hybrid Mode (РЕКОМЕНДУЕМЫЙ) - Оптимальный баланс
```bash
npm run dev -- --mode=hybrid --seed=42
```

**Что происходит:**
- LLM reasoning используется **только на критических событиях** (price spikes, EV urgency)
- Heuristics для обычных шагов (быстро и эффективно)
- **14.5% экономия** ($1.08 savings) vs baseline контроллера
- **1.60 kW peak reduction** (27% снижение пиковой нагрузки)
- 13/48 LLM calls (27% использования) - умная комбинация

**Преимущества:**
- ✅ Лучшая экономия среди всех режимов
- ✅ Стабильный результат на разных seed'ах
- ✅ Production-ready (быстрый + надёжный)

**Когда использовать:**
- Для демо судьям (показывает лучший результат)
- Для production deployment
- Когда нужна стабильность + производительность

---

### 2. LLM Mode - Максимальный AI
```bash
npm run dev -- --mode=llm --seed=42
```

**Что происходит:**
- Все 3 агента (Interpreter, Planner, Executor) используют LLM reasoning
- Mock LLM провайдер парсит промпты и генерирует "умные" JSON ответы
- **-0.2% результат** (ограничен EV deadline override для гарантии compliance)
- 48/48 LLM calls для каждого агента

**Когда использовать:**
- Для демонстрации AI reasoning архитектуры
- Для тестирования prompt engineering
- Демонстрация AI capabilities
- Максимальная оптимизация
- Обкатка LLM промптов перед prod

---

### 3. Heuristic Mode - Быстрые правила
```bash
npm run dev -- --mode=heuristic --seed=456
```

**Что происходит:**
- Все агенты используют **детерминистические правила**
- Нет LLM calls вообще (0/48)
- **4.8% экономия** vs baseline
- Моментальное выполнение (<100ms)

**Когда использовать:**
- Baseline comparison
- Понимание "разумного минимума" без AI
- Быстрый отклик + качество на важных моментах

---

### 3. Heuristic Mode - Максимальная скорость
```bash
npm run dev -- --mode=heuristic
```

**Что происходит:**
- Все агенты используют детерминированные эвристики
- Нет LLM вызовов вообще
- **6.4% экономия** vs baseline
- 0/48 LLM calls
- Мгновенная работа

**Когда использовать:**
- Тестирование без LLM
- Fallback режим если LLM недоступен
- Baseline для сравнения с LLM

---

## 📊 Что показывает в логах

```
[Step 30] [🧠 LLM] Analyzed energy system state. Tariff at $0.75/kWh...
[Step 30] Selected Plan A based on price spike urgency. Battery strategy: discharge...
  Action: Batt=-3.5kW EV=0.0kW HVAC=25.5°C
  Result: Cost=$0.00 Grid=-2.3kW Penalty=$0.00 Comfort=0.8°
  ⚠️  Safety issues filtered: SOC_TOO_LOW at +4, SOC_TOO_LOW at +5, ...
```

**Разбор:**
- `[🧠 LLM]` - агенты использовали LLM reasoning
- `[HEURISTIC]` - агенты использовали эвристики
- **Action** - что система решила сделать:
  - `Batt=-3.5kW` → разряжаем батарею (отдаём в сеть)
  - `EV=0.0kW` → не заряжаем электромобиль
  - `HVAC=25.5°C` → целевая температура
- **Result**:
  - `Cost=$0.00` - на этом шаге не платим за энергию!
  - `Grid=-2.3kW` - экспортируем в сеть (отрицательное потребление)
- **Safety issues** - планы которые нарушили ограничения и были отфильтрованы

---

## 🎯 Ключевые моменты сценария

### Шаг 0-10: Cheap overnight tariff ($0.14)
- EV charging начинается
- Батарея может заряжаться дёшево

### Шаги 26-28: Cloud event
- Солнечная генерация падает на 70%
- Система должна компенсировать другими источниками

### Шаги 30-31: ⚡ TARIFF SHOCK ($0.75/kWh)
- **Критический момент!**
- LLM агенты выбирают Plan A: агрессивная разрядка батареи
- Система **экспортирует энергию** в сеть (Grid отрицательный)
- Cooking spike усложняет ситуацию (+1.2 kW базовой нагрузки)

### Шаги 32-36: Evening peak ($0.42)
- Всё ещё дорого, но не критично
- Балансировка между комфортом и ценой

### Дедлайн EV: до шага 28
- Электромобиль должен зарядиться 10 kWh
- Агенты выбирают оптимальное время для зарядки

---

## 🔍 Итоговая статистика

```
============================================================
🎯 IMPROVEMENT (Baseline - Agent)
============================================================
  Cost Saved          : $2.78
  Peak Reduced        : 2.57 kW
  Comfort Improved    : -1.36°C·steps
  Penalties Reduced   : $0.00

============================================================
🧠 AI AGENT STATISTICS
============================================================
  Mode                : LLM
  LLM Interpreter calls: 48/48
  LLM Planner calls   : 48/48
  LLM Executor calls  : 48/48

✅ Agent achieved 37.3% cost reduction vs baseline!
```

**Что значит:**
- **Cost Saved**: сколько сэкономили vs тупого baseline контроллера
- **Peak Reduced**: насколько снизили пиковую нагрузку (важно для grid limits)
- **Comfort**: небольшое увеличение дискомфорта (допустимый tradeoff)
- **LLM calls**: сколько раз каждый агент использовал LLM

---

## 🔧 Дополнительные параметры

### Seed - воспроизводимость
```bash
npm run dev -- --mode=llm --seed=42
npm run dev -- --mode=llm --seed=999
```

Seed контролирует:
- Небольшие вариации в natural language reasoning
- Всё ещё детерминированные решения
- Полезно для A/B тестирования

---

## 🐛 Troubleshooting

### "Cannot find module"
```bash
npm run build
```

### Нет изменений после правок кода
```bash
# Dev mode автоматически перезапускается (tsx)
npm run dev

# Или пересобрать
npm run build && npm start
```

### Нужна помощь с CLI
```bash
npm run dev -- --help
# Или просто посмотреть код parseArgs() в src/index.ts
```

---

## 📚 Дальнейшее изучение

- **REFACTORING.md** - полное техническое описание архитектуры
- **README.md** - общий обзор проекта
- **src/llm/mockProvider.ts** - как работает mock LLM
- **src/agents/orchestrator.ts** - как координируются агенты

---

## 🚀 Готово!

Теперь у вас работающая AI-агентная система для управления энергией! 

Попробуйте разные режимы и seeds, смотрите как меняются решения и результаты. 

**Следующий шаг:** Замените MockLLMProvider на OpenAI/Claude (см. REFACTORING.md) 🎯
